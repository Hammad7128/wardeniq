Phase 0 test baseline — generated in a fresh venv (Python 3.11) built from
app/requirements.txt + app/requirements-dev.txt, against the working tree AFTER
the 2.1 pre-flight change (app/workers.py moved out of app/), BEFORE any Phase 1+
restructuring. dbintegration tests were not run (no live MongoDB available in
this sandbox) — run those separately per ci.yml.

Command: pytest -m "not dbintegration"
Result:  486 passed, 30 failed, 11 errors, 1 skipped, 29 warnings (3.68s)

All 41 failures/errors are confined to exactly 6 files, all for the SAME root
cause — NOT a regression, a pre-existing gap:

  tests/test_contracts_auth007.py                        (11 errors)
  tests/test_contracts_full_suite_false_positives.py       (2 failed)
  tests/test_contracts_orphaned_reads.py                   (6 failed)
  tests/test_coverage_contract_cap.py                      (7 failed)
  tests/test_grounding_contract_tier.py                    (6 failed)
  tests/test_mindmap_orphaned_cap.py                       (9 failed)

Root cause: these files load fixture data from tests/fixtures/auth_pilot/*
(patches, .py.txt snapshots, a JSON dataset) via a local _read() helper. That
directory was NEVER committed to git (git ls-files tests/fixtures/ is empty)
and is currently empty on disk (a prior local investigation appears to have
cleaned it up — see WARDENIQ_CROSS_FILE_IMPLEMENTATION_TEST_RESULTS.md). A
fresh clone or CI checkout hits the identical FileNotFoundError, e.g.:

  FileNotFoundError: [Errno 2] No such file or directory:
  '.../tests/fixtures/auth_pilot/pr9_tokens.patch'

Action needed (independent of the refactor): commit tests/fixtures/auth_pilot/
to git, or these 6 files stay red in CI for anyone starting from a clean
checkout. Everything else — all 9 RBAC suites, test_api_routes, test_api_exec,
test_scanners, test_store_degradation, test_index_fingerprint (all 8 subprocess-
boundary tests), test_non_implementation_files, sheet import, usage, s3_storage,
etc. — passes cleanly with zero changes needed.

Tooling baselines (both informational per ci.yml, not merge gates):
  ruff check app tests            -> 78 errors  (see ruff.txt)
  mypy app --ignore-missing-imports -> 146 errors in 17 files (see mypy.txt)

Docker baseline: NOT RUN. This sandbox has the docker CLI (29.4.3) but no
daemon socket (/var/run/docker.sock absent) — `docker build` cannot execute
here under any circumstances. Run locally before Phase 1:
  docker compose config --quiet
  docker build -f app/Dockerfile .
