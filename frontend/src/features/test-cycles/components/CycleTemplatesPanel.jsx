/** CycleTemplatesPanel for test-cycles. */
export default function CycleTemplatesPanel() {
  return (
    <div className="card">
      <h2>Cycle templates</h2>
      <div className="sub">Reusable case sets — save any cycle as a template, then spin up new cycles from it.</div>
      <div id="cyc-templates"></div>
    </div>
  );
}
