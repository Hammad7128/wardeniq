/** ValidatorEmptyState for validator. */
export default function ValidatorEmptyState() {
  return (
    <div className="card" id="val-no-feature">
      <h2>MCQ Validator</h2>
      <div className="muted">Please select a feature first to run the requirements validator.</div>
    </div>
  );
}
